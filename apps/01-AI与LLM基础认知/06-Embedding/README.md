# Demo · Embedding

对应：[模块 01 · Embedding](../../../docs/学习模块/01-AI与LLM基础认知/06-Embedding.md)

本条必须看见的：同一组词，Token ID 相减没有语义；玩具向量的余弦分数能排出「宠物更近猫/狗、更远石头」。

不调嵌入 API。二维坐标是手写的教学用数，不是 MiniMax / OpenAI 的真实向量。

```bash
cd apps
yarn install
yarn app:01-06-embedding
```

不消耗额度。
