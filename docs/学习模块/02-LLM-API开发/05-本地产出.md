# **本地产出**

> 对应模块：[模块 02 · LLM API 开发 ⭐⭐⭐⭐⭐](./README.md) · 最后一条（模块级收口，对照 README **验收**）
> **来源**：本对话（详解 4.1~4.9 + demo 跑通 + 收口补 --no-stream + cost）
> **状态**：已沉淀（收口）

**一句话**：本模块最终交付 = `apps/02-LLM-API开发/` 下各小节 Demo（Streaming / 协议 A vs B / AbortController / Rate-Limit）。模块 00 mini-app 仍只是 00 的三入口。

---

## 这一课解决什么问题

模块 00 / 01 已经能调通 API、能理解 Token；模块 02 要把「**从代码层面真正调用模型，而不是在网页里聊天**」这一刀全部走完：流式 + 非流式两种模式都要能跑、中途能取消、能处理 429 / 超时 / 网络错、能算成本、协议 A 和 B 都要实践。

---

## 验收对照（README 验收标准 6 条 → 代码落点）

| # | 验收项 | 状态 | 落点 |
| - | ------ | ---- | ---- |
| 1 | **流式和非流式两种模式都实现** | ✅ | [`01-Streaming-SSE`](../../../apps/02-LLM-API开发/01-Streaming-SSE/)（`/api/stream` vs `/api/blocking`）；模块 00 mini-app CLI 默认流式 |
| 2 | **前端/CLI 能逐字渲染流式输出** | ✅ | 同上 Demo 的浏览器页；mini-app CLI `stdout.write` + `yarn app:00-01-mini-server` 的 SSE 页 |
| 3 | **实现了中途取消（AbortController）** | ✅ | [`03-AbortController`](../../../apps/02-LLM-API开发/03-AbortController/)（三端点对照）。**不**塞进模块 00 mini-app |
| 4 | **每次请求后打印 input / output token + 估算成本** | ✅ | mini-app CLI 打 `usage`；429 / 成本时间线见 [`04-Rate-Limit`](../../../apps/02-LLM-API开发/04-Rate-Limit/) |
| 5 | **处理 429 / 超时 / 网络错误三种异常** | ✅ | [`04-Rate-Limit`](../../../apps/02-LLM-API开发/04-Rate-Limit/)（mock 五场景 + 真 API）。**不**在 mini-app 里再开一份 retry |
| 6 | **协议 A 与协议 B 至少各实践过** | ✅ | [`02-协议-A-vs-B`](../../../apps/02-LLM-API开发/02-协议-A-vs-B/)；mini-app 双 CLI：`yarn app:00-01-mini-cli-a` / `yarn app:00-01-mini-cli-b` |

---

## 本地拆步对照（README 本地拆步 5 步 → 代码落点）

| # | 拆步 | 落点 |
| - | ---- | ---- |
| 1 | Streaming / 非流式对照 | ✅ [`01-Streaming-SSE`](../../../apps/02-LLM-API开发/01-Streaming-SSE/) |
| 2 | HTTP + SSE（模块 00 三入口已有一份；本条机制用小节 Demo） | ✅ mini-app `server.ts` + [`01-Streaming-SSE`](../../../apps/02-LLM-API开发/01-Streaming-SSE/) |
| 3 | AbortController：能中途取消 | ✅ [`03-AbortController`](../../../apps/02-LLM-API开发/03-AbortController/) |
| 4 | 429 / 超时 / 网络错误三条分支 | ✅ [`04-Rate-Limit`](../../../apps/02-LLM-API开发/04-Rate-Limit/) |
| 5 | 协议 A vs B | ✅ [`02-协议-A-vs-B`](../../../apps/02-LLM-API开发/02-协议-A-vs-B/) + `yarn app:00-01-mini-cli-a` / `yarn app:00-01-mini-cli-b` |

---

## 当前代码地图（收口后）

### CLI 入口

```text
cd apps
yarn app:00-01-mini-cli-a "你好"              → 协议 A 流式
yarn app:00-01-mini-cli-a --no-stream "你好"  → 协议 A 一次性
yarn app:00-01-mini-cli-b "你好"              → 协议 B 流式
yarn app:00-01-mini-server                    → HTTP + SSE（端口 3000）
yarn app:02-01-streaming-sse                  → 本模块 Streaming Demo
yarn app:02-04-rate-limit                     → 本模块 429 Demo
```

每次跑完会打印：

```text
Token 用量: { prompt: 182, completion: 70, total: 252 }
估算成本：¥ 0.000252（单价 0.001 元/1k token，由 MINIMAX_PRICE_PER_1K 控制）
```

### 文件职责（[mini-app README](../../../apps/00-环境准备/01-mini-app/README.md)）

| 文件 | 职责 |
| ---- | ---- |
| `src/index.ts` | 协议 A CLI（模块 00 最小流式入口） |
| `src/index-anthropic.ts` | 协议 B CLI（对照入口，00 不验收） |
| `server.ts` | HTTP + SSE 服务端（§5.3 koa） |
| `public/index.html` | 浏览器聊天 UI |
| `apps/load-root-env.ts` | 共用 dotenv 加载 `apps/.env` |
| `apps/.env` | `MINIMAX_*` 运行时配置 |

---

## 收口时补的（`coach next` 时发现缺口）

进入本地产出行时对照验收：流式 / 非流式、取消、429 都落在 **本模块各小节 Demo**，不要灌进模块 00 mini-app。mini-app 只保留 00 的三入口。

详见 [mini-app README](../../../apps/00-环境准备/01-mini-app/README.md)。

---

## 我追问过的（本对话内）

- **`demo:02-rate-limit` 里 `02` 是模块还是章节？** → 应改为 `app:{模块}-{小节}-{短名}` 规范；进维护模式，全仓库 8 个 demo 改名 + AGENTS.md §5.2 同步。位置：本对话维护模式段
- **真 API demo 撞不出 429 怎么办？** → 加 `/api/real`（单次）+ `/api/real-burst`（并发 20 撞限，`BURST=1` 才跑）；MiniMax-M3 个人 Key 配额宽裕，20 并发只占 ~2 RPS，撞不出。位置：[apps/02-LLM-API开发/04-Rate-Limit/index.ts](../../../apps/02-LLM-API开发/04-Rate-Limit/index.ts)
- **Rate-Limit 要不要灌进模块 00 mini-app？** → 不要。429 只落在 [`04-Rate-Limit`](../../../apps/02-LLM-API开发/04-Rate-Limit/) 小节 Demo。
- **本地产出进 `coach next` 时缺什么？** → 对照本模块 README 验收 + 各小节 Demo 已落；mini-app 子文件夹 `README.md` 跟上。

---

## 取舍

- **流式 vs 非流式默认走哪个**：默认流式（用户体验好，TTFT 短）；非流式靠 `--no-stream` 显式开。**取舍点**：流式优先，但成本可控 / 自动化 / 批量任务场景必须能用非流式
- **`MINIMAX_PRICE_PER_1K` 默认 0.001**：占位价；实际单价按 vendor 控制台填。**取舍**：精度 vs 配置成本——粗估够用就行，不要把全模型价目表塞 env
- **3 秒自动 abort 留在流式**：非流式不走（一次性返回没法中途取消）。**取舍**：模拟用户体验 vs 真实非流式场景——前者更贴近真实，后者更纯净
- **`printUsageAndCost` 收口补 vs 04-Rate-Limit 那条补**：放在本地产出，因为 04 那条「能讲清 Rate-Limit」就够了，「打印成本」是验收级而非 Rate-Limit 概念级

---

## 踩坑（本对话实际撞过的）

- **`yarn app:00-01-mini-server` 端口冲突**：多次跑没关进程，5176 / 3000 端口被占。`lsof -ti :5176 | xargs kill -9` 清掉
- **`NodeJS.HttpIncomingMessage` 类型不存在**：[`04-Rate-Limit/index.ts`](../../../apps/02-LLM-API开发/04-Rate-Limit/index.ts) 用了 `NodeJS.HttpIncomingMessage["headers"]`，实际是 `IncomingMessage`（从 `node:http` import）
- **`RETRYABLE_STATUS` 声明但未使用**：第一次写分类时显式列了，代码只查 NON_RETRYABLE_STATUS；删了显式集合，注释写清"隐式可重试"
- **`result: T | string` 类型统一**：成功路径 body 是 `T`（流 / 非流对象），失败路径 body 是 `string`（错误信息）；`RequestFn<T>` 用 `body: T | string` + 成功时 `as T` 断言
- **`yarn app:00-01-mini-cli-a --no-stream` 第一次跑没看到 retry 输出**：正常——单次请求没 429，attempts=1，`logRetry` 跳过；要看 retry 链路得在 burst 模式或真 API 撞限
- **真 API burst 没撞出 429**：MiniMax-M3 个人 Key RPM 配额宽裕；20 并发只占 ~2 RPS；要撞得 50+ 并发或换更小模型

---

## 代码运行流程（对本模块当前切片）

```text
yarn app:00-01-mini-cli-a 你好                    →  /tmp/no-stream.log 实测一次（见下）
yarn app:00-01-mini-cli-a --no-stream 你好，1+1=? →  /tmp/no-stream.log 实测一次（见下）
```

实测输出（`yarn app:00-01-mini-cli-a --no-stream 你好，1+1=?`）：

```text
> 你好，1+1=?

[协议 A · OpenAI Chat Completions · MiniMax-M3 · no-stream]

Assistant: <think>The user is greeting me in Chinese and asking a simple math question...

你好！1 + 1 = 2。😊
这是一个很基础的算术问题。如果你还有其他问题，欢迎随时提问！

Token 用量: { prompt: 182, completion: 70, total: 252 }
估算成本：¥ 0.000252（单价 0.001 元/1k token，由 MINIMAX_PRICE_PER_1K 控制）
```

---

## 还没搞懂的（下个模块前必须解决）

- **流中途 429 怎么接**：`for await` 拿到一半 chunk 后服务端抛 429，前端是丢弃重连还是标记续写？上一节 [04-Rate-Limit.md 流式 + 429](./04-Rate-Limit.md) 已讲方案；生产接法留给模块 19 / 21 的小节 Demo。
- **`MINIMAX_PRICE_PER_1K` 真实单价**：占位价够本模块讲清「要算成本」；实际按 vendor 控制台填。学到模块 19 成本那块再校准。
- **多轮对话**：现在 `messages` 只有 user 一条；模块 06 多轮对话与 Context 用自己的小节 Demo 加 system / assistant。

---

## 过关自检（对照验收 6 条）

合上代码，必须能讲清：

1. **流式 vs 非流式差在哪**：`stream:true` vs 不传；流式 `for await` 逐 chunk，非流式一次性 `ChatCompletion`
2. **AbortController 在三条路径怎么用**：流式 CLI = 首个 chunk 后 3s；协议 B = 首个 `text` 事件后 3s；HTTP = 首个 `res.write` 后 3s；signal 走 SDK options 第二参
3. **可重试 vs 不可重试分类**：429 / 408 / 5xx / 网络错 退避；400 / 401 / 403 / 404 / 422 不重试
4. **成本估算公式**：`cost = total_tokens / 1000 × MINIMAX_PRICE_PER_1K`；单价 env 化
5. **协议 A vs B 字段差异**：`chat.completions`（OpenAI）vs `messages.create`（Anthropic）；role 位置 / usage 字段名映射；详见 [02-协议-A-vs-B 小节 MD](./02-协议-A-vs-B.md)
6. **SSE 帧格式**：`data: {json}\n\n`，结束帧 `data: [DONE]\n\n`；chunk 是 zod 类实例要 `JSON.parse(JSON.stringify(...))` plain 化
